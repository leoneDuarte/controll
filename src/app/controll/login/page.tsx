import { ControllLoginForm } from '@/features/controll/components/controll-login-form';

export const metadata = {
  title: 'Controll: Login'
};

export default function ControllLoginPage() {
  return <ControllLoginForm />;
}
