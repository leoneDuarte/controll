import { ControllRegisterForm } from '@/features/controll/components/controll-register-form';

export const metadata = {
  title: 'Controll: Register'
};

export default function ControllRegisterPage() {
  return <ControllRegisterForm />;
}
